"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = convertTextToJson;

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var colorMap = {
  '0': 'black',
  '1': 'dark_blue',
  '2': 'dark_green',
  '3': 'dark_aqua',
  '4': 'dark_red',
  '5': 'dark_purple',
  '6': 'gold',
  '7': 'gray',
  '8': 'dark_gray',
  '9': 'blue',
  'a': 'green',
  'b': 'aqua',
  'c': 'red',
  'd': 'light_purple',
  'e': 'yellow',
  'f': 'white'
};

function convertTextToJson(text) {
  var prefix = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '§';
  var component = {
    text: '',
    extra: []
  };
  var re = new RegExp("".concat(prefix, "[0-9a-fklmnor]"), 'gm');
  var unformattedText = text.replace(re, '');
  var currentFormattings = {};
  var i = 0;
  var lastPos = 0;
  var match;

  while ((match = re.exec(text)) != null) {
    i++;
    var realIndex = match.index - 2 * i + 2;
    var doneText = unformattedText.substring(lastPos, realIndex);

    if (doneText.length > 0) {
      component.extra.push(_objectSpread(_objectSpread({}, currentFormattings), {}, {
        text: doneText
      }));
    }

    switch (match[0][1]) {
      case 'k':
        currentFormattings.obfuscated = true;
        break;

      case 'l':
        currentFormattings.bold = true;
        break;

      case 'm':
        currentFormattings.strikethrough = true;
        break;

      case 'n':
        currentFormattings.underlined = true;
        break;

      case 'o':
        currentFormattings.italic = true;
        break;

      case 'r':
        currentFormattings = {};
        break;

      default:
        // color code (resets preceding format codes)
        currentFormattings = {
          color: colorMap[match[0][1]]
        };
        break;
    }

    lastPos = realIndex;
  }

  component.extra.push(_objectSpread(_objectSpread({}, currentFormattings), {}, {
    text: unformattedText.substring(lastPos)
  }));
  return component;
}